OUTPUT output 00000

# We'll probably need some envvar support here.
FROM clientuser:supersecure@$SNAPPER:5432/cachedb/test_pg_mount:v1 IMPORT fruits AS my_fruits, vegetables

SQL CREATE TABLE join_table AS SELECT fruit_id AS id, my_fruits.name AS fruit, vegetables.name AS vegetable FROM my_fruits JOIN vegetables on fruit_id = vegetable_id